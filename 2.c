#include<stdio.h>
//total due 404
int main()
{
	 int x, pro,num,cash;
	 float average, grade, sum;

		printf("Enter no. of Products: ");
		scanf("%d", &pro);
	
	while(cash <= cash){
			printf("Insufficient Cash, Try Again: ");
			scanf("%d", &num);
			}
		printf("\n");
		
		
		for(x=1; x<=pro; x++){
			printf("Enter price for product %d:", x);
			scanf("%d", &num);
		
			sum=sum+pro;
		}
		
		printf("Enter Cash:");
		scanf("%d",&cash);


		printf("\n");
		
	

//		average = sum / num;
//		printf("\nClass Average: %.2f", average);
}
